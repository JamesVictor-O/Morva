# Magic (magic.link) publishable key — powers sign in / sign up with Magic
# Embedded Wallets across the app. Get one from https://dashboard.magic.link
# (Settings → API Keys → Publishable key). Without this set, sign-in shows a
# friendly "not configured" error instead of crashing.
NEXT_PUBLIC_MAGIC_PUBLISHABLE_KEY=

# Particle Network project credentials — construct the buyer's Universal
# Account (EIP-7702 mode) used to read their real cross-chain balance.
# Get these from https://dashboard.particle.network (same three values the
# backend SDK's MorvaConfig.particle expects). Without all three set, the
# balance UI shows "Balance unavailable" instead of crashing.
NEXT_PUBLIC_PARTICLE_PROJECT_ID=725f8ce3-0ee3-4910-92c9-319714f39475
NEXT_PUBLIC_PARTICLE_CLIENT_KEY=
NEXT_PUBLIC_PARTICLE_APP_ID=

# Supabase project — Postgres (via Drizzle, see src/lib/db) + Storage (stall
# and product photo uploads, see src/lib/storage.ts). Get these from your
# Supabase project's Settings → Database (connection string — use the
# "Transaction pooler" one) and Settings → API (URL + service_role key).
# Server-only: never prefix these NEXT_PUBLIC_, the service role key
# bypasses Storage RLS entirely.
DATABASE_URL=
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Signs merchant session cookies (see src/lib/auth/session.ts) — any long
# random string, e.g. `openssl rand -base64 32`. Rotating it invalidates
# every existing merchant session (forces re-verification).
SESSION_SECRET=
